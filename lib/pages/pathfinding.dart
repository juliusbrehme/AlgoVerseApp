import 'package:flutter/material.dart';

class PathFindingPage extends StatelessWidget {
  const PathFindingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("PathFinding");
  }
}