import 'package:flutter/material.dart';

class TreeSearchPage extends StatelessWidget {
  const TreeSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("TreeSearch");
  }
}